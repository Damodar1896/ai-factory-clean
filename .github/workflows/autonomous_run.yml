name: 24/7 Distributed Cloud Autonomous Pipeline

on:
  schedule:
    - cron: '0 */4 * * *'
  workflow_dispatch:

jobs:
  distribute-workload:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r automation_core/requirements.txt

      - name: Run Multi-Cloud Verification & Pipeline Engine
        env:
          SUPABASE_URL: ${{  postgresql://postgres:[SP#*&$NG%^!*@RK/%$]@db.zhdrghjygatcqsyjmvdl.supabase.co:5432/postgres }}
          SUPABASE_KEY: ${{ eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InpoZHJnaGp5Z2F0Y3FzeWptdmRsIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc4NTA0MTU2MiwiZXhwIjoyMTAwNjE3NTYyfQ.siqScmP8G0RVCMLw_bOtxe8fRAEc5V_TgZZuIkHUD9s }}
        run: |
          python automation_core/deploy_manager.py
          python automation_core/pipeline_engine.py
